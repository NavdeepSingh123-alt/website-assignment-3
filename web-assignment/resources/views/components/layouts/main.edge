<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>@!section('title', 'Expenses App')</title>

  @vite('resources/js/app.js')
  @vite('resources/css/app.css')

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    crossorigin="anonymous"
  />
</head>

<body>
  <header class="top-container">
    <div class="container d-flex justify-content-between align-items-center nav-items">
      <div>
        <ul class="d-flex gap-3 m-0 p-0">
          <li><a href="/">Home</a></li>
          <li><a href="/categories/create">Create category</a></li>
          <li><a href="/expenses/create">Create products</a></li>
        </ul>
      </div>
      <div>
        <nav>
          <ul class="d-flex gap-3 m-0 p-0">
            @if(auth.user)
              <li>{{ auth.user.fullName }}</li>
              <li>
                <form id="logout-form" action="/logout" method="POST">
                  {{ csrfField() }}
                  <input type="hidden" name="_method" value="DELETE" />
                  <button class="logout-btn btn btn-link text-white p-0" type="submit">Logout</button>
                </form>
              </li>
            @else
              <li><a href="/login">Login</a></li>
            @end
          </ul>
        </nav>
      </div>
    </div>
  </header>

  {{{ await $slots.main() }}}
</body>

</html>
